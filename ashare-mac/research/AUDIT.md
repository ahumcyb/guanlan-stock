# 实验执行记录

2026-09-05：首次开发预检发现中性占位名称 `historical` 含有 `st`，误触发原分类器的名称排除规则，全部策略及基准均无候选、无成交。该次记录保存在 `.cache/strategy-research/run-20260905` 并以 `INVALIDATED.json` 明确作废；没有打开留出期，也没有观察到任何有成交的收益。

修复仅把中性名称替换为“研究股票”，数值规则与切分不变；同时禁止零成交基准产生可选的相对排名。新增正向非空股票池与空基准回归测试，重建 `panel-v2.npz`，新运行独立保存到 `run-20260905-v2`。

新面板预检：329 个研究交易日中，2025-05-06 尚不足 80 根历史报价，按既定规则为空仓；其余 328 日都有合法股票池及 5 只成交额基准候选。完整 407 日面板包含 3,232 个历史沪深主板代码。此次检查只看覆盖率与候选数量，不计算收益。

v2 完成开发与选择阶段后，独立审查补充“基准有成交但候选全空”的防御条件：零成交候选也不得形成相对排名。v2 所有候选均有数百笔成交，此条件不影响实际选型或收益；最后区间仍未启用。为保持源码哈希可复现，保留 v2 记录，以相同面板冻结 `run-20260905-v3`，逐项确认开发与选择统计完全一致后，才检验已选定的 `momentum_20`。本次没有调整任何选股公式、参数或时间切分。
