# 每日收盘总结验收 · 2026-09-07

## 已启用的行为

交易日北京时间 16:10 自动更新目标日收盘行情、计算四套策略、保存市场与策略摘要，再调用 DeepSeek 并通过 Bark 提醒。当前自动总结、AI、完成提醒均开启，模型为 `deepseek-v4-pro`。盘中 AI 仍维持原来的关闭状态。Key 只保存在服务器私密配置，App 中可更换，公开 API、报告和 Git 均不包含 Key。

## 实际运行

- 首次启用在 2026-09-07 收盘前，因此补生成最近已收盘的 2026-09-04；下一次计划时点为 2026-09-07 16:10。初次补生成不代表用 9 月 7 日盘中行情替代 9 月 4 日。
- 任务 `a303bb18-dfb8-4b62-83c9-2cbb58175add` 携带 `purpose=daily_review`、`expected_as_of=20260904`，由 `mac` 执行并完成发布。
- ProMax 在 2026-09-07 10:35:58 +08:00 重新获取目标日三表：daily 5,548 行、adj_factor 5,556 行、stk_limit 7,781 行，无更新失败。收盘证明、行数和规范化内容哈希经打包与服务端发布再次验证。
- 四策略共用 generation `20260907T104227-3f9215`，行情 revision `20260904-c0eccf474694bb2b`。新抓取目标日内容与原收盘快照一致，因此数据 revision 没有改变，收盘抓取证明与报告 generation 是新的。
- 沪深个股口径为 5,207 只：上涨 2,249、下跌 2,773、平盘 185；成交额 20,304.41 亿元；涨停 41、跌停 9。MA20 基础池宽度为 45.74%，上涨/下跌家数比为 0.811，两者分别计算。通过原始 Parquet 独立复算和证据哈希读回。
- 流动性趋势、缩量回踩转强、黄金坑、60 日动量的精选数量分别为 10、10、4、5。第四策略精选为 2 涨 3 跌；数量在请求 AI 前计算，模型不再自行计数。
- 最终 DeepSeek 分析为 `ready`，标题为「饲料相对较强，半导体相对较弱」。正文家数、比率、行业涨跌和四策略数量与证据一致；未来观察使用条件表述。实测发现的无数据支持推断由范围检查拦截，回退时仍保留数字摘要，不自动重复收费。
- `/v1/daily` 的 latest 与 `/v1/daily/20260904` 完全一致。Bark 事件为 `accepted`、attempts 为 1，链接为 `guanlan://daily?date=20260904`。手动修正 AI 后没有再次发送同日期完成提醒；这代表推送服务接受，不代表本次已由用户在实体手机上确认接收。

## 客户端与检查

- Mac 1.5.0（6）已安装到 `/Users/bennie/Applications/观澜选股.app` 并验证签名。首页/侧栏入口、总结状态、API Key 安全输入框和保存自动开关已通过真实窗口操作。
- iOS 1.4.0（6）模拟器构建、真机签名构建通过。模拟器上的首页入口和设置界面已验证。
- 最终模型结果发布后，从运行中的 Mac 和 iPhone 模拟器的 App 缓存回读，日期、`ready` 状态、V4 Pro 模型以及整份 latest 均与服务器完全一致；Swift 原生模型及离线缓存测试使用真实最终 API 内容。
- Python 全量 192 项测试通过，覆盖指定收盘日、三表证明、实际 Parquet 打包发布、重启幂等、API 授权、私密配置、AI 失败与越界回退。Mac、iOS 模拟器和签名设备构建通过。
- 实体 iPhone 本次不在线，实际设备应用查询失败，未安装 build 6。已有配对和开发者模式保持，待设备恢复连接后更新。Mac 后续锁屏，最终正文的视觉截图和实体手机通知直达仍待解锁/重连验证；后台与原生缓存读回已完成。

服务端运行代码核验至 `2befcf5`。完整每日功能上线前的回滚包为 `/opt/guanlan-backups/before-daily-20260907.tar.gz`；恢复旧代码时的队列字段兼容步骤见每日总结运行说明。
