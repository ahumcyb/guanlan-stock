# 2026-09-08 升级验收

本次完成 ProMax 实时回退修复、昨日精选结算及第四项左侧日线策略。验收发生于 9 月 8 日凌晨，最新完整日线为 **2026-09-07**。

## 发布与运行

- 服务端运行代码 `1a9e7dee4a7c26c5bf8d7fb1910a801a725241c6`，76 个源文件及测试文件 SHA-256 全部一致；三个 systemd 服务 active，Mac 两个后台节点在线。
- 数据版本 `20260907-dc220c08f23f9a86`，研究版本 `20260908T002847-52c1e0`。当日日线 5,549 行，报告可检索 5,613 只；市场事实另按沪深 A 股范围过滤，口径不混用。
- Mac 构建的四策略完整收盘包上传后，由服务器独立验证并发布。HTTPS 回读的四报告与本地输出逐字节一致，精选数量分别为 10 / 10 / 2 / 10；新第四项为 `left_rebound`。
- 退役 `momentum_60` 接口仍返回 `20260907T161943-41e78a`，报告哈希校验通过。两处版本清理均保护最新有效旧策略报告和其共享 K 线；回归覆盖超过 24 小时、连续四代新策略发布。
- 升级备份 `/opt/guanlan-backups/before-left-settlement-20260908-v2.tar.gz`。首次备份发现服务器没有 research 目录，部署在修改运行代码前中止并恢复服务；随后按实际存在目录完成备份并成功升级。

## 昨日精选结算

发布新版本之前已冻结 `20260905T224439-14a5ce` 中的 9 月 4 日原四策略精选。快照 SHA-256：`3461c3ffdb646123dd8731c40cc034f851cb15d612b4190a4ec7e5594ea2e702`。评价日临时补算的 9 月 4 日版本没有被当成昨日已知信号。

| 原策略 | 9 月 4 日精选数 | 9 月 7 日可结算数 | 等权复权观察涨跌 |
|---|---:|---:|---:|
| 流动性趋势 | 10 | 10 | −2.135673% |
| 缩量回踩转强 | 10 | 10 | −0.419533% |
| 黄金坑 | 4 | 4 | +0.083918% |
| 原 60 日风险调整动量 | 5 | 5 | −1.080218% |

29 只股票的涨跌和四组均值，已使用本机日线及复权因子独立重算，与线上结果一致。新左侧策略明确没有此前精选样本。这里未扣交易费用或模拟成交，不是账户净收益。

9 月 7 日总结已用 `refresh_facts=true` 重新生成，状态 ready，模型 `deepseek-v4-pro`。实际正文准确引用上表及涨跌家数；本日新选 10 / 10 / 2 / 10 只明确留待下一交易日评价。Bark 仍只有原来的 `daily-20260907` 成功通知记录（accepted，attempts=1），没有重复发送。

## 客户端与测试

- 本机与 Linux 服务器各 **228 项 Python 测试通过**。覆盖 ProMax 显式分批、完整带时点回退、失败分阶段记录、缺数/拆股结算、不可变精选、API 兼容和历史版本保留。
- Swift 模型/离线缓存/CSV/专用 HTTPS 证书/报告哈希测试通过；旧日报与实际新日报均可解码，新日报 29 条结算通过严格模型验证。
- Mac **1.6.0（7）**构建、签名验证通过，已替换安装，旧 App 已备份。
- iOS **1.5.0（7）**模拟器和个人签名真机构建通过。真机安装成功，实时设备列表读回版本与 build 均正确。
- 两台设备当时锁屏。iPhone 启动被系统明确以 Locked 拒绝，Mac 界面检查也因锁屏不可用；没有把安装成功表述为已完成新版真机界面或新缓存验收。解锁后打开 App 会按现有刷新逻辑同步已发布结果。

## 实时回退验收范围

实时接口用明确代码批次请求 ProMax；缺失或无效时点的记录只由完整新鲜的备用报价替换，不拼接价格与外源时间。ProMax / 新浪真实 HTTPS 传输及源时间字段已验证；夜间旧报价被正确拒绝。

本轮手动检查由 Mac 执行，使用 `20260907-dc220c08f23f9a86`，前一交易日 9 月 7 日，正常返回“当前不在 14:30–14:50 策略时段，已准备历史特征”。这验证了本机缓存与发布链，未声称完成下一次交易时段的全市场实时验收。

左侧策略的完整规则和首次负收益检验结果见 [左侧低吸](LEFT_REBOUND.md)。
