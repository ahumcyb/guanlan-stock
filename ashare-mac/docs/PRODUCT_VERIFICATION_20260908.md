# 2026-09-08 产品优化与底部放量验收

完成用户指定的优化 1 / 2 / 3 / 6，修复当天实时提交失败，并新增交易日 14:30 底部放量三倍观察。手机按用户要求暂不安装更新。

## 已上线

- 服务器运行代码 `2fd2e6453a7cc04c2ac7dd3da95db0c15ce09721`，87 个运行源文件和测试文件哈希全部一致。Linux 上 257 项 Python 测试通过，API、计算调度、实时调度均 active。
- Mac 1.7.0（8）已安装。原生 HTTPS 使用与手机相同的固定服务器、TLS 1.2 以上、专用证书与禁止重定向策略。补充了该私有 CA 对应的单域 ATS 配置，未改为信任任意证书或任意主机。
- 最新完整日线为 2026-09-08；数据版本 `20260908-41c30659b9046461`，四策略发布版本 `20260908T162059-834bf5`。Mac 当前四报告清单与线上逐项相等，缓存报告 SHA-256 均校验通过。
- 已现场查看新首页：10 只候选、最近精选观察、09-09 14:30 下次检查；股票池统计默认收起。K 线显示本次入选日期、位置参考和失效参考。切换左侧策略直接展示同代缓存结果，未触发选股重算。
- 盘前不会把上一完整交易日误报为落后；服务端 market_status 按交易日历及 15:10 边界确定 expected_as_of。离线核验过期时显示待核验。

## 自选与通知

- 两个独立原生 WatchlistReplica 通过专用 HTTPS 验证增删往返同步。临时测试条目已移除，原始自选列表恢复。离线操作、重试、迟到响应及跨设备合并另有测试。
- 线上 13 个历史轮次和 6 条原始提醒逐项回读；通知 ID 一致，缺少旧 run_id 的提醒返回原消息和空 report，没有使用最新结果替代。
- 用真实 MobileStore 注入延迟请求复现“旧通知 A 晚返回覆盖新选择 B”，修复后新选择和关闭详情两个异步场景通过。
- 运行时已迁移旧记录；最多保存 1,000 个实时明细与 3,000 条通知原文。客户端保留当前四报告、上一代和历史动量缓存，避免无界增长。
- 今天的失败根因及网关边界实测见 [实时故障说明](REALTIME_FAILURE_20260908.md)。修复后 8,313 字节假租约请求可达业务层并返回 409；普通接口同尺寸仍 413；超过 256 KiB 仍 413。
- 修复后的夜间真实手动检查由 Mac 执行并成功发布 waiting 状态，消息明确等待策略时段。原始计算结果现在会在本机保留最近 30 份；提交失败单独记录 publication 与 HTTP 状态。

## 每日总结

已重新核验并生成 9 月 8 日总结，DeepSeek V4 Pro 返回 ready，且通过新原生模型/缓存验证。

- 与 9 月 7 日相比，成交额变化 +0.7362%，上涨家数增加 217，市场宽度增加 3.9853 个百分点。
- 名单新增 / 两期均入选 / 移出：流动性趋势 10 / 0 / 10；缩量回踩 8 / 2 / 8；黄金坑 2 / 0 / 2；左侧低吸 7 / 3 / 7。
- 观察结算仍使用 9 月 7 日已保存精选，未改为用今日新选股票评价今日收益。移出原因来自当日同版本报告，并逐只核对收盘价与已验证行情一致。
- `daily-20260908` 仍只有原成功通知记录，accepted、attempts=1，没有重复推送日报。

## 底部放量

[规则说明](BOTTOM_VOLUME.md)：14:30 独立检查，最新价在前 60 个完整交易日低价上方 0%–10%，累计量达到前五个完整交易日全天均量至少三倍。显示命中总数和按倍数排序的前十只，通知正文含名称、代码和倍数。

原两策略字典保持兼容，新内容在可选 bottom_volume 字段中；指数失败不阻止已经有效完成的底部结果，后续轮次不覆盖 last_bottom。规则、调度、缺数、过时、边界、排序和通知集成测试通过。下一次计划执行为 **2026-09-09 14:30（上海时间）**。

## 验证边界

iOS 1.6.0（8）源码与模拟器构建通过，按用户要求未安装到手机；新策略通知正文兼容旧手机提醒列表。底部策略尚未经历上线后的首个交易时段，不宣称已完成实盘盈利验证。没有用收盘数据补造当天尾盘名单。

Mac 首页、价格线、数据日期、策略切换及新版日报已实际读取。界面自动化随后出现 native pipe closed，重连未恢复，因此实时历史弹窗未完成最后的逐项图形验收；对应 API、模型、异步路由与数据回读均已验证。App 进程正常运行。
