# K 线升级验收 · 2026-09-12

## 交付范围

Mac 1.10.0（13）与 iOS 1.9.0（13）共用图表模型、指标计算和 Canvas 渲染：

- 日 / 周 / 月 K，完整开高低收、涨跌幅、振幅、成交量与成交额；MA10/20/60 可切换。
- 日期与价格十字线、平移缩放、回到最新、信号定位、逐根导航、独立量能 / MACD / RSI 副图。
- 连续价 / 原始价明确区分；历史提醒原价按原日期换算，缺日不移到邻近日期。历史总结先截断至结算日，再计算图形及指标。
- 两端大图保留周期、价格口径、指标、窗口和选中日期；支持响应式横竖屏布局与深浅色刻度。
- 实时提醒、日报昨日精选、日报昨日实时收益、当日候选逐股进入同一图表。
- 加载请求身份隔离；较旧请求的成功、错误、取消不能覆盖新选择。有效缓存受代码、日期、generation、data revision 共同校验。
- 新接口最多500根，旧120根接口继续可用。原归档无扩展时明确回退，不伪造原始价或成交额。

## 数据核验

本次使用本机已校验的 `20260911-ba1f88ba0eaf3545` 日线快照重新生成图表，没有重新请求市场行情。当前数据截至 **2026-09-11**，常见股票实际412根日线，起始2025-01-02；新上市股票按自身可用历史显示。

完整校验 **5,613 个扩展文件、2,244,849 根日线**：日期、OHLC、量额、均线、连续价的逐日链和末日原价锚点通过。四策略 sidecar 一致，扩展末120根与旧接口逐值一致。

结果包 generation 为 `20260912T132052-7d81f7`，ZIP **205.5 MiB / 11,241 个成员**，外层展开285.5 MiB，计入内层 gzip 的实际累计展开699.1 MiB。上传256 MiB、ZIP展开512 MiB、实际嵌套展开1 GiB等限额保持受控。压缩图表最多128 KiB，单次解压最多512 KiB。

包 SHA-256：`56f73ac06368d699999e0eff43220d80e0a8a74979788933baf67f6b168a9b3a`。

## 自动验证与复核

- Mac Python：**309项通过**；服务器 Python：**309项通过**。
- 原生完整产品测试通过：四策略缓存、自选并发、通知路由、底部放量归档、每日模型、图表数学、历史截断、价格定位、请求竞态与扩展缓存。
- 独立只读代码复核通过；首次复核指出的连续价链验证与嵌套gzip预算问题已补回归并修复。
- Mac 发布构建、iOS模拟器构建、真机SDK无签名构建通过。
- Mac实际预览检查了日/周、MACD、大图保留状态、窗口放大与向历史平移、深色刻度。
- iOS模拟器以公开行情缓存检查股票详情、OHLC量额与原日期查价。触控采用识别前方向判断，纵向交给页面滚动、横向浏览、长按查价。首次最终手势和横屏复验曾被 Mac 锁屏中断；续装时补验模拟器全屏与横屏布局通过，大图保持原选中日期。触控手感仍需实际使用反馈，模拟器验证不等于真机全流程验收。

## 部署与安装

服务器运行时代码为 `a4590ddbd1d3e58ddff1534769b86f79a43b2b7f`，98个源文件/测试/依赖文件逐一验 SHA。API 在系统 Python 中不导入 pandas / pyarrow；三个服务恢复 active。部署前备份位于 `/opt/guanlan-backups/before-kline-20260912.tar.gz`。

结果包通过正常 Mac 任务租约上传，服务器已校验并激活四策略 generation `20260912T132052-7d81f7`，任务状态 `completed`。Swift 原生客户端经固定证书 HTTPS 回读确认：20260911、412根、原始价可用；四套策略原固定版本的120根接口仍可读。发布前后9月11日日报和实时历史归档逐值一致，Mac与服务器心跳均在线；Mac Datta批量+D6、服务器ProMax的配置未变。

Mac新版本已写入 `/Users/bennie/Applications/观澜选股.app`，包签名验证通过，运行路径仍指向本机主工程；旧版本备份在本地私有发布目录。锁屏期间未重新启动界面。

iOS **1.9.0（13）已于9月12日下午通过局域网安装到配对 iPhone，并成功启动**。设备应用列表回读确认版本与构建号一致，启动后再次确认进程仍在运行。原免费描述文件于当天14:51到期，恢复Xcode账号后已续签至 **2026-09-19 16:28:50（上海）**；包签名、描述文件中的设备、应用标识与开发证书匹配校验全部通过。手机在用户完成开发者信任后解除启动拦截。未修改应用凭据、签名团队或策略配置。

本次不改盘后/实时策略规则、不改 Mac 优先与服务器接管策略、不登录服务器 Datta 客户端、不触发历史提醒补发。
