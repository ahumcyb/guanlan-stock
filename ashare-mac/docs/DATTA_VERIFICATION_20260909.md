# 2026-09-09 达塔接入验收

观澜的 Mac 主节点已启用达塔 D6，覆盖实时股票 / 沪深300报价、1分钟线及日线更新。名录、行业、交易日历、复权因子、涨跌停价与参考股本继续使用原参考数据接口。

## 实际验证

- DataInterface 76.0.0 已安装到本机用户应用目录，用户完成系统首次启动确认与登录。当前登录、权限和数据服务均有效；自启项已修正为永久安装路径，避免使用 macOS 临时 AppTranslocation 位置。
- 全市场盘后采集请求 4,985 只，取得 4,983 只，耗时 49.409 秒。对可匹配股票核对收盘价；接口千分位编码产生的 0.001 偏差按 A 股分币精度归一化。
- 2026-09-08 日线取得 5,548 条，现有数据为 5,549 条。匹配股票收盘价全部一致；不足部分未伪造。日线采集及整理约 118 秒，行情请求部分约 55 秒。
- 浦发银行连续 50 根日线与现有未复权数据完全一致，期间有两种复权因子，确认没有把前复权价格再次复权。北交所 920000.BJ 的日线与报价也已抽验。
- 浦发银行的当日 1 分钟历史返回 240 根。报价使用个股 marketDate / dataTimestamp；不使用代理发送时间代替股票行情更新时间。
- 272 项 Python 测试在 Mac 与 Linux 服务器通过；原生四策略缓存、自选、通知历史、异步路由、日报模型测试通过。
- Mac 1.8.0（10）已安装并启动；iOS 1.7.0（10）已完成安装，启动时设备锁定，不能把安装成功视为本次真机界面验收。

## 主备部署边界

实际双机登录会使 Mac 的客户端退出登录。服务器客户端虽已安装、授权及启动成功，历史 K 线可用，但 D6 实时报价持续返回上游失败。当前保留可验证的运行方式：

- Mac：达塔 D6，继续优先计算并发布结果。
- 服务器：ProMax / 新浪原备用行情，仅在 Mac 失联后接管计算。
- Linux 达塔服务已停止并关闭自启，避免踢掉 Mac 登录。新安装的两个端口已限制为服务器本机访问。
- Linux 包需要较新 glibc；已从签名校验通过的 Ubuntu 软件仓库提取独立 2.39 运行库，以服务私有挂载提供，没有替换服务器系统 glibc。

服务器运行提交 `b716e41790183652bbd55a146b4bf4f66c2df002`，90 个源文件及测试文件哈希核验通过。发布包 SHA-256：`4f83e638ed99ee7e4b1dd6ef7d4889606a8f3a3ac3126a00902b916ec0abfe10`。旧运行代码备份为 `/opt/guanlan-backups/before-datta-20260909.tar.gz`。

专用证书 HTTPS 回读确认：Mac 在线、服务器备用就绪、实时策略与手机通知启用；下一次检查为 2026-09-09 14:30。原两实时策略和“2.5倍且当日上涨”的底部观察条件保持原定义。

本次盘后数据用于接入校验，没有冒充过去 14:30 的快照，也没有补发过时股票提醒。新的交易时段时效仍由每轮来源时间和覆盖检查决定。
