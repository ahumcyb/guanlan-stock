# 达塔 v85 切换验收（2026-09-16）

## 已生效

- 行情在线入口仅为达塔，ProMax 构造入口及配置回退已禁用。历史数据保留。
- Mac、Linux 实际客户端均为 v85；客户端原生自启动关闭，由统一登录监督服务管理。
- 默认 Mac 登录、计算；失联宽限结束后 Linux 才能登录。Mac 返回时，正在执行的 Linux 任务保留登录权，结束并确认客户端退出后交接。
- Linux 旧直接登录服务已 mask；Linux 数据端口与管理端口保持本机访问范围。

## 实际数据发布

- 最新已收盘日：2026-09-15。
- 行情版本：`20260915-8eb9516bbfb499c9`。
- 目标日有效日线 5,545 行、连续因子 5,545 行、限制价参考 5,562 行；原打包检查通过日线代码的完整因子/限制价覆盖。
- 当前名录 5,564 行，包含经过校验的流通股本与 `shares_date`。不把停牌、未上市或未通过量额校验的条目补成虚构行情。
- 报告发布版本：`20260916T002616-feed85`；四套策略均为 2026-09-15，入选数依次为 0 / 0 / 0 / 10。
- 收盘总结和 DeepSeek 分析均返回 ready。提醒已由 Bark 接受，未以服务端接受状态冒充手机实际到达证明。

此次两次耗时计算因登录代次变化而被栅栏取消；原日志不足以确定每次底层故障的具体类型。新增短断线宽限、同机客户端修复保持代次及结构化日志，并进行了受控恢复验证。最终恢复使用保存的实际收盘数据和三套完成报告，补完第四套，再通过新任务租约上传。原收盘采集证明保持 `2026-09-15T23:43:31.924260+08:00`，未把恢复时间伪装为重新采集时间；数据、四套报告和图表重新通过服务器验证后才发布。

## 双机与恢复验证

- 暂停 Mac 节点后，Linux 在接管窗口后独占登录；Linux D6 日线样本与 D4 名录样本均成功返回。
- Linux 使用与 Mac 相同的完整行情输入，重算四套当日选股；代码与排序摘要 SHA256 均为 `11dd0e391c5d7352d9dd3cb76722a5850358612e8d693c4f1054a035a2b02415`。该实机检查覆盖取数和当日选股，未重复生成 Linux 全量历史检验与 K 线导出。
- 独立测试覆盖 Linux 活跃盘后/实时任务阻止抢占、迟到 epoch 拒绝、并发续约/发布与换主、停止未确认时持续保留登录权。
- Mac 数据服务停止后的实机恢复经过 active → recovering → active，全程保持 epoch 6；Linux 没有登录抢占。
- 生产运行代码版本 `9fbe743`，108 个源码/测试/依赖文件校验一致。本机与 Linux 均通过 353 项测试。

## 最终运行状态

Mac 登录权为 active，Mac/服务器计算心跳在线；Linux 登录监督服务待命，原生数据客户端停止。Mac 两份行情镜像均已同步至上述版本；用正式发布数据准备 9 月 16 日实时策略，得到 4,977 只有效股票，全部具备前日股本，无缺数警告。

私有操作证据保留在 `.cache/datta85-release/`，包括 API 读回、恢复证明、完整结果包、双机选股摘要和客户端恢复轨迹。凭据、数据包和日志不进入 Git。手机读取原有兼容接口，此次无需重新安装或签名。
