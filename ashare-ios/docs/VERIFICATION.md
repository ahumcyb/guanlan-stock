# 验收记录 · 2026-09-05

## 真机交付

- Xcode 26.6，使用个人 Apple ID 的 Personal Team 自动签名。
- iPhone 17 Pro，iOS 26.6.1：配对、开发者模式、个人开发者信任完成；安装及启动成功。
- 签名验证通过，描述文件包含当前 iPhone，首份描述文件到期时间为 **2026-09-12 14:51:32（北京时间）**。
- 私有连接文件已通过开发工具直接传入 App 沙盒。真机第一次成功导入后，临时 `Documents/Pairing.guanlan` 已删除；访问凭据保存在设备钥匙串。
- 从真机 App 沙盒回读报告：SHA-256 和大小正确；5,611 只股票及 1 / 3 / 5 日统计与 Mac 本地结果一致；中国银行 120 根 K 线也完全一致。

## Mac 优先与服务器接管

数据日期 **2026-09-04**，版本 **20260904-c0eccf474694bb2b**。

| 验证 | 结果 |
|---|---|
| 公网 HTTPS 443 | 阿里云安全组规则已生效；专用证书校验连接成功 |
| Mac 在线任务 | Mac 接管，计算两策略并上传，服务器校验后发布 |
| Mac 离线任务 | 停止本机后台节点，确认心跳离线，服务器接管 ProMax 更新及两套策略计算 |
| 更新覆盖 | ProMax 核对目标 20260904，未补齐日期为 0；本次无需新增行情日 |
| 数据一致性 | Mac 和服务器两套策略各 5,611 行、1 / 3 / 5 日统计、分月统计和抽检 K 线完全一致 |
| 恢复主节点 | 已重新启用本机 LaunchAgent；API 回读 Mac 在线、服务器备用就绪 |
| 结果版本 | 最终研究版本 20260905T145518-42ce2b，两套策略绑定同一行情版本 |

本次 Mac“重新选股”从提交到发布约 4 分 10 秒；服务器修正运行配置后，“更新并选股”从接管到发布约 8 分 57 秒。这是本次数据与负载下的运行记录，包含的更新步骤不同，不作为通用性能承诺。

服务器最初的 `vm.swappiness=0` 配合 `MemoryHigh` 导致回收停顿、心跳租约失效。修正为 `swappiness=60`，移除 `MemoryHigh`，保留 `MemoryMax=1300M` 与 4 GiB swap 后完成整个任务，`oom` / `oom_kill` 均为 0。应用服务使用独立低权限账号，代码目录不可写。

## 编译与交互验证

- iOS 模拟器构建、未签名真机 SDK 构建、个人团队签名真机构建均通过。
- 原生模型 / 缓存 / CSV 测试通过；真实 HTTPS 清单及报告 SHA-256 回读通过。
- Python **82 项测试通过**，包含 Mac 优先、租约接管、迟到上传拒绝、发布后禁止修改、跨挂载发布、ZIP 路径与摘要篡改拒绝。
- 模拟器真实交互通过：全市场 001979 搜索、两套策略切换、30/60/120 日图表、触摸查看日期、加入观察并回读、历史检验及数据状态。CSV 系统分享面板识别文件，导出文件回读为 11 列，股票代码及行数正确。
- 进度条布局已修正，计算期间不会遮住返回和星标按钮。
- 独立代码复核已完成，未发现剩余阻断项。实际手机、worker 和 ProMax 凭据未进入暂存代码；私有配置权限为 600 且被 Git 忽略。

## 研究限制

软件工程与部署验收通过，不代表策略具有稳定收益优势。当前两套模型仍属探索性研究，未完成独立样本外与模拟实盘验证；历史页面保留成本、缺数、重叠事件和名单偏差说明。观察列表首版分别保存在各设备，尚未跨设备同步。
