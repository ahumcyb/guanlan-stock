# 观澜图标

采用深青底色、浅玉色水波和金色行情柱，呼应“观澜”与股票研究；不使用文字，保留小尺寸可辨识的轮廓。

`AppIconMaster.png` 是 2026-09-05 按用户更换图标的要求生成并选用的原始图片。图像生成后保存在工程内，供后续构建使用。

运行 `swift scripts/create-icon.swift`，通过 ImageIO 等比缩至 1024 × 1024，再交由 Xcode 生成各设备尺寸。图标为不透明正方形，圆角由 iOS 处理。旧版 AppKit 绘制方式输出了全黑图片，现改为读取已确认的图片并校验像素内容。
